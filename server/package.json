{
  "name": "physics-hub-whatsapp-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Authenticated bulk WhatsApp gateway for Meta Cloud API, generic relays, and optional WhatsApp Web sessions.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "mock": "WA_PROVIDER=mock node src/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.22.2",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
